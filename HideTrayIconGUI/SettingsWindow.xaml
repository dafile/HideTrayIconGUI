<Window x:Class="HideTrayIconGUI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置" Width="420" Height="350"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False" Background="#F5F5F5">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="过滤进程列表" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,4"/>
        <TextBlock Grid.Row="1" Text="以下进程不会显示在列表中 (每行一条，进程名):"
                   Foreground="#666" Margin="0,0,0,6"/>

        <TextBox Grid.Row="2" x:Name="FilterList" AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" FontSize="13" Padding="4"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="保存" Click="SaveSettings_Click" Width="80" Margin="0,0,10,0"/>
            <Button Content="取消" Click="CancelSettings_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
