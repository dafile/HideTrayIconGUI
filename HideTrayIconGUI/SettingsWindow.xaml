<Window x:Class="HideTrayIconGUI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置" Width="420" Height="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        ShowInTaskbar="False" Background="#F5F5F5">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Close behavior -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="窗口行为" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,6"/>
            <CheckBox x:Name="MinimizeToTrayCheck" Content="点击关闭按钮时最小化到托盘 (而非退出程序)"
                      FontSize="14" Margin="0,0,0,4"/>
        </StackPanel>

        <Separator Grid.Row="1" Margin="0,0,0,12"/>

        <!-- Filter -->
        <TextBlock Grid.Row="2" Text="过滤进程列表 (不显示在主列表中, 每行一条)" FontSize="15"
                   FontWeight="SemiBold" Margin="0,0,0,6"/>

        <TextBox Grid.Row="3" x:Name="FilterList" AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" FontSize="13" Padding="4"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,10,0,0">
            <Button Content="保存" Click="SaveSettings_Click" Width="80" Margin="0,0,10,0"/>
            <Button Content="取消" Click="CancelSettings_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
